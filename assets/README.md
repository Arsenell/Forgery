# Assets

This folder is reserved for textures, background images, icons, and supporting media.

The current build uses generated CSS atmosphere and Three.js particles, so no additional image assets are required to run the site.
