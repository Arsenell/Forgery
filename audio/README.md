# Audio

Optional audio files can be placed here:

- `ambient.mp3`
- `hover.mp3`
- `sword-unsheath.mp3`

The app includes procedural Web Audio fallbacks, so the viewer still works if these files are not present.
